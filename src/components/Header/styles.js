import styled from 'styled-components';

export const HeaderPoke = styled.header`
    position: relative;
    display: flex;
    justify-content: center;
    align-items: center;
    flex-flow: column;
    height: 100px;
    padding: 20px;
    background-color: rgb(255, 27, 27);
    box-shadow: 0 0 6px rgba(0, 0, 0, 0.2);
`;
